<!DOCTYPE HTML>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ashvak Govindarajula - Data Portfolio</title>

    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Sora:wght@300;400;600;700&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">

    <!-- Font Awesome Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">

    <style>
        /* General Styles */
        body {
            margin: 0;
            font-family: 'Sora', sans-serif;
            background-color: #FAF3EB; /* Soft Beige */
            color: #141414; /* Dark Text */
            line-height: 1.6;
        }

        /* Navigation Bar */
        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 80px;
        }

        .logo a {
            font-size: 2rem; /* Matches Owkin */
            font-weight: 700;
            text-decoration: none;
            color: #141414;
            letter-spacing: -1px;
        }

        .menu-icon {
            font-size: 1.5rem;
            cursor: pointer;
        }

        /* Hero Section */
        #hero {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 100px 120px;
            height: 80vh;
        }

        .hero-text {
            max-width: 50%;
        }

        /* Tagline & Text */
        h1 {
            font-size: 4rem; /* 48-64px */
            font-weight: 700;
            margin-bottom: 10px;
            letter-spacing: -1px;
        }

        .tagline {
            font-size: 1.8rem; /* 24-32px */
            font-weight: 400;
            color: #444;
            font-family: 'Inter', sans-serif;
            margin-bottom: 20px;
        }

        p {
            font-size: 1.2rem; /* 16-20px */
            color: #333;
            max-width: 550px;
            font-family: 'Inter', sans-serif;
        }

        /* Arrow + Call to Action */
        .arrow {
            display: flex;
            align-items: center;
            font-size: 1.3rem;
            font-weight: 600;
            margin-top: 30px;
        }

        .arrow i {
            font-size: 1.5rem;
            margin-right: 10px;
        }

        /* Button */
        .button {
            display: inline-block;
            padding: 12px 24px;
            border: 2px solid #141414;
            border-radius: 50px;
            font-size: 1.1rem;
            font-weight: 600;
            color: #141414;
            transition: all 0.3s ease;
            margin-top: 20px;
            font-family: 'Inter', sans-serif;
        }

        .button:hover {
            background: #141414;
            color: #fff;
        }

        /* Dynamic Data Animation */
        .data-visual {
            font-size: 1.4rem;
            font-weight: 600;
            color: #4f9fff;
            font-family: 'Inter', sans-serif;
            animation: fadeInOut 3s infinite alternate;
            text-align: right;
        }

        @keyframes fadeInOut {
            from { opacity: 0.5; }
            to { opacity: 1; }
        }

        /* Responsive Design */
        @media (max-width: 900px) {
            .navbar {
                padding: 20px 40px;
            }

            #hero {
                flex-direction: column;
                text-align: center;
                padding: 60px;
                height: auto;
            }

            .hero-text {
                max-width: 100%;
            }

            .data-visual {
                margin-top: 30px;
                text-align: center;
            }
        }
    </style>
</head>
<body>

<!-- Navigation -->
<header class="navbar">
    <div class="logo"><a href="https://yourwebsite.com">Ashvak Govindarajula</a></div>
    <div class="menu-icon"><i class="fas fa-bars"></i></div>
</header>

<!-- Hero Section -->
<section id="hero">
    <div class="hero-text">
        <h1>Unleashing the Power of Data</h1>
        <div class="tagline">Transforming raw numbers into valuable insights.</div>
        <p>
            I design <strong>scalable data pipelines</strong>, build <strong>intelligent dashboards</strong>,  
            and drive <strong>business impact</strong> through <strong>data engineering & analytics</strong>.
        </p>
        <div class="arrow">
            <i class="fas fa-arrow-down"></i>
            <span>Explore My Work</span>
        </div>
        <a href="#projects" class="button">Explore My Work</a>
    </div>

    <!-- Dynamic Data Animation -->
    <div class="data-visual">
        📊 "Data isn't just numbers, it's a story waiting to be told."
    </div>
</section>

</body>
</html>
